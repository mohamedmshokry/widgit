some inforamtion that were checked in after init
need pull this file down
additional configurations
found a couple more config values that needs change

additional configurations
found a couple more config values that needs change
